import React from "react";
import ReactLoading from "react-loading";

const LoadingIcon = () => {
    return (
        <div>
            <ReactLoading color="#001733" height={125} width={80} />
        </div>
    )
  
};

export default LoadingIcon;